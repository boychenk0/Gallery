.container-fluid
  .row-fluid
    .span12
      #categories_show
        #categories_show-t.text-center
          %h3
            =@category.name.capitalize
        #portfolio
          -@images.each do |img|
            .item
              =link_to "#{@category.id}/images/#{img.id}" do
                =image_tag img.url, :alt => img.url
              %span
                Likes:
                =img.likes_count
                Comments:
                =img.comments_count
        .btn-group.paginate
          = paginate @images, :window => 1
      -##images-list
      -#  #category_title.text-center
      -#    = @category.name
      -#  - @category.images.each do |img|
      -#    #image-item
      -#      =link_to "#{@category.id}/images/#{img.id}" do
      -#        =image_tag img.url, :alt => img.url


    -#.span3
    -#  -cache("categories_content/#{user_signed_in? ? current_user.cache_key : ''}") do
    -#    =render 'categories/categories_menu'
    -#.span9
    -#  #title_images
    -#    = @category.name.capitalize
    -#  #images
    -#    #images-sorted
    -#      -#sorted by
    -#      -#=link_to 'created', root_path
    -#      -#=link_to 'likes', root_path
    -#      -#=link_to 'comments', root_path
    -#    -cache("images_list/#{@category.cache_key}") do
    -#      #images-list
    -#        = render 'images/images_list'
    -#      .btn-group.paginate
    -#        = paginate @images, :remote => true, :window => 1