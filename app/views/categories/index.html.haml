.container-fluid
  .row-fluid
    .span12
      #categories_list
        #categories_list-title.text-center
          %h3
            Categories
        #sort
        =render 'categories/categories_list2'
        .btn-group.paginate
          = paginate @categories, :window => 1
      .row-fluid
        .span6
          #likes_list
            #likes_list-t
              %strong Last 5 likes
            #likes_list-content
              =render 'categories/last_likes'
        .span6
          #comments_list
            #comments_list-t
              %strong Last 5 comments
            #comments_list-content
              =render 'categories/last_comments'
    -#  -cache("categories_content/#{user_signed_in? ? current_user.cache_key : ''}") do
    -#    =render 'categories/categories_menu'
    -#.span9
    -#  #title_images
    -#    =t('.all_image')
    -#  #images
    -#    #images-sorted
    -#      -#sorted by
    -#      -#-#=link_to 'created', root_path
    -#      -#-#=link_to 'likes', root_path
    -#      -#-#=link_to 'comments', root_path
    -#    -cache("images_list/all_images") do
    -#      #images-list
    -#        =render 'images/images_list'
    -#      .btn-group.paginate
    -#        = paginate @images, :remote => true, :window => 1