.img_show
  = image_tag @image.url, :height => 650, :width => 650, :class => 'img-polaroid'
  #like
    = link_to "#" do
      = image_tag 'like.jpg'
  = pluralize(@image.likes_count, 'like')

  %div{id:"pv_like_wrap", class:"fl_l", onclick:"Photoview.like()", onmouseout:"Photoview.likeOut()", onmouseover:"Photoview.likeOver()"}

#comments
  - @comments.each do |comment|
    %p.text-warning

    %alert.alert-info= comment.body
    -#.text-warning
    -#  #{comment.user.email}
    -#  = time_ago_in_words comment.created_at
    -#  ago
    -#  -#- if current_user == comment.user
      -#  = link_to "Delete comment", [@post, comment], :method => :delete, :confirm => "Are your sure?"
- if user_signed_in?
  = form_for ([@image, @image.img_comments.new]) do |f|
    %p= f.text_area :body
    %p= f.submit "Add comment", :class => "btn"

